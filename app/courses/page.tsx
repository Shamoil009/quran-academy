import React from "react";
import Courses from "@/components/Courses";
import Layout from "@/components/Layout";

const page = () => {
  return (
    <Layout>
      <Courses />
    </Layout>
  );
};

export default page;
